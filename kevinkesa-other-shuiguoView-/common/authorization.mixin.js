// 无需登录校验的页面
const noLoginPage = [
	'pages/shop/index'
];
module.exports = {
	// onShow() {
	// 	let pages = getCurrentPages();
	// 	let page = pages[pages.length - 1];
	// 	console.log(page.is)
	// 	if (page && !this.vuex_token && !noLoginPage.includes(page.is)) {
	// 		this.$u.route('/pages/kcx/login/index')
	// 	}
	// }
}
